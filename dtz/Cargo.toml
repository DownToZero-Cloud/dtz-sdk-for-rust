[package]
name = "dtz"
version = "0.2.1"
edition = "2021"
authors = ["jens@apimeister.com"]
description = "base crate for the DownToZero Cloud API"
license = "MIT OR Apache-2.0"
rust-version = "1.63"

[features]
containers = []
core = []
identity = []
full = []
objectstore = []
observabilty = []
rss2email = []

[dependencies]
dtz-config = { version = "0.1", path = "../dtz-config" }
dtz-containers = { version = "1", optional = true, path = "../dtz-containers" }
# dtz-core = { optional = true, path = "../dtz-core" }
# dtz-identity = { optional = true, path = "../dtz-identity" }
# dtz-objectstore = { optional = true, path = "../dtz-objectstore" }
# dtz-observability = { optional = true, path = "../dtz-observability" }
# dtz-rss2email = { optional = true, path = "../dtz-rss2email" }
